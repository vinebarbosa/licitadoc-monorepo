## 1. Documentation Scope

- [x] 1.1 Inventory the current API entrypoints, plugins, modules, shared auth/helpers, database layers, scripts, and tests that the new guides must describe
- [x] 1.2 Define the final section structure for `apps/api/agents.md` and `apps/api/architecture.md` so each document has a clear maintenance purpose

## 2. Document Authoring

- [x] 2.1 Create `apps/api/agents.md` with local commands, workflow expectations, change boundaries, file placement guidance, and validation steps for backend work
- [x] 2.2 Create `apps/api/architecture.md` with the current runtime flow, plugin and module responsibilities, database boundaries, OpenAPI generation path, and test architecture
- [x] 2.3 Add lightweight discovery links from the repository documentation if that is needed to help contributors find the new API guides quickly

## 3. Review and Alignment

- [x] 3.1 Verify the new API guides reference current source files, commands, and extension points instead of aspirational or outdated structure
- [x] 3.2 Review the docs from a contributor and agent perspective to ensure the guidance is concise, actionable, and aligned with the existing backend workflow
