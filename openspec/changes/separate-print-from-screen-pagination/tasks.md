## 1. Confirm Current Failure

- [x] 1.1 Reproduce the sparse-page print/PDF behavior with the affected document or an equivalent fixture.
- [x] 1.2 Confirm that the blank space is caused by automatic `placement: "before"` pagination boundaries being treated as print page breaks.
- [x] 1.3 Confirm manual page-break boundaries still need to force physical pages in print/PDF output.

## 2. Separate Screen and Print Boundary Behavior

- [x] 2.1 Update `createPaginationBoundaryCss` in `apps/web/src/modules/documents/ui/document-pagination-surface.tsx` so automatic boundaries keep screen spacing and screen page breaks.
- [x] 2.2 Update the automatic-boundary print CSS generated by `createPaginationBoundaryCss` so it resets spacer/margin but does not force `break-before` or `page-break-before`.
- [x] 2.3 Keep manual `placement: "self"` boundary print CSS forcing `break-after` / `page-break-after` while resetting visual spacer height.
- [x] 2.4 Update global print selectors in `apps/web/src/styles.css` so `[data-document-pagination-break-before="true"]` resets spacing in print without re-forcing a page break.
- [x] 2.5 Ensure screen preview frames, page counts, and automatic boundary displacement remain unchanged.

## 3. Test Coverage

- [x] 3.1 Update pagination surface unit tests to assert automatic boundaries do not emit forced print page breaks.
- [x] 3.2 Update pagination surface unit tests to assert manual boundaries still emit forced print page breaks.
- [x] 3.3 Add or update preview regression coverage for a short topic followed by normal content so automatic print boundaries cannot create mostly blank pages.
- [x] 3.4 Keep existing pagination model tests passing without changing document generation semantics.

## 4. Browser and PDF Verification

- [x] 4.1 Run focused web tests for document preview pagination and print CSS.
- [x] 4.2 Run the web typecheck.
- [x] 4.3 Verify the running app on `localhost:5173` by printing/exporting the affected document or equivalent fixture to PDF.
- [x] 4.4 Inspect the generated PDF or browser print preview to confirm automatic boundaries no longer create pages that are mostly blank.
- [x] 4.5 Verify explicit/manual page breaks still start following content on a new printed page.

## 5. OpenSpec Completion

- [x] 5.1 Mark implementation tasks complete after code and verification pass.
- [x] 5.2 Run final OpenSpec status for `separate-print-from-screen-pagination`.
