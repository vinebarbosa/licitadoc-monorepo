## 1. Status Badge Parity

- [x] 1.1 Update the process-detail status metadata so the approved badge icon choice is defined alongside the existing status label and styling.
- [x] 1.2 Render process-detail document badges with the approved confirmation, clock, and warning icons for `concluido`, `em_edicao`/`pendente`, and `erro` states.

## 2. Duplicate Action Parity

- [x] 2.1 Restore the copy icon in the `Duplicar` overflow menu item for process-detail document cards.
- [x] 2.2 Preserve the current process-detail routes and document action behavior without introducing a new duplication workflow.

## 3. Verification

- [x] 3.1 Add or update focused process-detail page tests for the status badge iconography and duplicate menu presentation.
- [x] 3.2 Run the relevant web test command for the process detail page.