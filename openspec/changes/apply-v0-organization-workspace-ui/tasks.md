## 1. v0 Reference Extraction

- [x] 1.1 Retrieve the v0 chat `dBy5w4jPvHh` and identify the `/organizacao` main content files.
- [x] 1.2 Extract the v0 mock data, overview page, tabs, forms, tables, department cards, and upload zones.

## 2. Workspace Implementation

- [x] 2.1 Replace the placeholder `OwnerOrganizationWorkspace` with the v0 main organization content, excluding the sidebar.
- [x] 2.2 Adapt imports, mock data, local state, and helper types to the existing Vite app.
- [x] 2.3 Add any missing local design tokens required by the v0 semantic color classes.

## 3. Tests And Verification

- [x] 3.1 Update owner organization page tests to cover the mock-backed v0 workspace behavior.
- [x] 3.2 Run focused web tests and typecheck/build checks for the organization workspace.
- [x] 3.3 Run OpenSpec status/validation for `apply-v0-organization-workspace-ui`.
