/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../../client";
import type { GetHealthQueryResponse } from "../models/GetHealth";

function getGetHealthUrl() {
  const res = { method: "GET", url: `http://localhost:3333/health` as const };
  return res;
}

/**
 * @summary Application health
 * {@link /health}
 */
export async function getHealth(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetHealthQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "GET", url: getGetHealthUrl().url.toString(), ...requestConfig });
  return res.data;
}
