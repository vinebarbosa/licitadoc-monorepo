/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../../client";
import type {
  ResetPasswordMutationRequest,
  ResetPasswordMutationResponse,
  ResetPassword400,
  ResetPassword401,
  ResetPassword403,
  ResetPassword404,
  ResetPassword429,
  ResetPassword500,
} from "../models/ResetPassword";

function getResetPasswordUrl() {
  const res = {
    method: "POST",
    url: `http://localhost:3333/api/auth/reset-password` as const,
  };
  return res;
}

/**
 * @description Reset the password for a user
 * {@link /api/auth/reset-password}
 */
export async function resetPassword(
  data: ResetPasswordMutationRequest,
  config: Partial<RequestConfig<ResetPasswordMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;

  const res = await request<
    ResetPasswordMutationResponse,
    ResponseErrorConfig<
      | ResetPassword400
      | ResetPassword401
      | ResetPassword403
      | ResetPassword404
      | ResetPassword429
      | ResetPassword500
    >,
    ResetPasswordMutationRequest
  >({
    method: "POST",
    url: getResetPasswordUrl().url.toString(),
    data: requestData,
    ...requestConfig,
  });
  return res.data;
}
